<%= form_for(@course) do |f| %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  </br>
  <div class="field">
    <%= f.label :producer %>
    <%= f.text_field :producer %>
    <%= f.select :producer_id, options_from_collection_for_select(User.where(role: "producer") , :id, :email) %>
  </div>
  </br>
  <div class="field">
    <%= f.label :location %>
    <%= f.select :location_id, options_from_collection_for_select(Location.all, "id", "name") %>
  </div>
  </br>
  <div>
    <%= f.label :program %>
    <%= f.select :program_id, options_from_collection_for_select(Program.all, "id", "name") %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
